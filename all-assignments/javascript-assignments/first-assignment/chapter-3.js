// first task
var age = 20;
alert(age);

// second task
var numOfVisits = 14;
alert("You have visited this site " + numOfVisits + " times");

// third task
var birthYear = 2002;
document.write(
  "My birth year is " +
    birthYear +
    "<br>" +
    "Data type of my declared variable is " +
    typeof birthYear
);

// fourth task
var visitorName = "John Doe";
var productTitle = "T-shirt";
var quantity = 5;
document.write(
  `<br><br><b>${visitorName}</b> ordered <b>${quantity} ${productTitle}</b> on XYZ Clothing store`
);
